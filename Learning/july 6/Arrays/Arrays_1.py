"""
Inserting Elements
Write a Python function that inserts the number 10 at the beginning of the array [2, 3, 4, 5].
"""

array = [2, 3, 4, 5]


def insert(array):
    array.inser(0, 10)
    return array


print(array)

"""
Given an array [1, 2, 3], use the append() method to add the string "hello" to the end of the array.
"""
array_1 = [1, 2, 3]


def append(array_1):
    array_1.append('hello')
    return array_1
print(array_1)

"""
Create a function that takes a list and an element, then inserts the element at the second-to-last position of the list.
"""
