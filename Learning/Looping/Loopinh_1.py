# Example of normal looping
"""total = 0
for x in [1, 2, 3, 4, 5, 6, 7, 8, 9]:
    total += x
    sum = total
    print(total)
print('sum is equal to', sum)"""

# another case example of looping using range() function
height = [5, 4, 7, 2, 3]

weightedSum = 0
"""
for i in range(len(height)):
    weightedSum += i * height[i]
    wightedSum_1 = max(height)
print('wightedSum', weightedSum)
print('wightedSum_1', wightedSum_1)"""

# example to use Enumeration() function
"""for i, h in enumerate(height):
    weightedSum += i * h
    print('weighted sum per Itteration', weightedSum,
          'Value of i per Itteration', i,
          'Value of h per itterration', h)

print("Total ", weightedSum)"""


